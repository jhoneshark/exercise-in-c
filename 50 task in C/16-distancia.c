#include <stdio.h>
#include <math.h>

int main() {
    int distancia, km;
    printf("Digite a distancia em km: ");
    scanf("%d", &km);

    distancia = (km * 2);

    printf("%d minutos",distancia);
    return 0;
}
